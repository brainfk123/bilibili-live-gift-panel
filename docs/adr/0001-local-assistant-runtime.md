# 答疑助手采用静态本地推理与独立签名模型制品

答疑助手采用 CPU-only llama.cpp 静态链接到主程序，模型则由用户确认后从 ModelScope 独立下载并通过内置 Ed25519 公钥验证签名清单。这比内嵌模型或启动本地模型服务更便于独立更新和单文件分发，同时避免开放本地推理端口；代价是原生崩溃会影响主进程，首版只承诺 Windows x64 CPU，并需保留推理 Interface 以便未来增加 GPU Adapter。
